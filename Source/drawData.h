#pragma once

#include <string>

	class drawData
	{
	private:
		
	protected:
		
	public:		
		drawData();
		~drawData();
		float _x,_y;
		float _rot;
		float _scaleX;
		float _scaleY;
		std::string _name;
	};

